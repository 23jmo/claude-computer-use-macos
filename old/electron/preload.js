/**
 * Preload script for Electron
 * Runs before the renderer process and can safely expose APIs
 */

// Currently no preload needed, but kept for future expansion
console.log('[Preload] Script loaded');

